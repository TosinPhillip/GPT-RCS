gpt-school-results/
├── app.py
├── config.py
├── models/          # Pydantic-like schemas or MongoEngine if needed
│   └── result.py
├── routes/
│   ├── admin.py
│   └── student.py
├── templates/       # Jinja2 HTML
│   ├── base.html
│   ├── admin/upload.html
│   └── student/search.html
├── static/
│   ├── css/style.css  # Tailwind or custom sleek CSS
│   └── js/main.js     # Smooth interactions (e.g., Fetch API)
└── utils/
    └── auth.py








Structure of the collections
Enrollments
{
  "_id": ObjectId(),
  "student_id": ObjectId("student core ID"),
  "session": "2025/2026",
  "term": "First",  // active term
  "class_": "JSS1",
  "subjects": ["Math", "English", "Physics"],
  "status": "active",  // active, alumni, left
  "teacher_comment": "Promoted from JSS1",
  "promoted_from": "JSS1"  // for tracking
}

Students
{
  "_id": ObjectId(),
  "admission_number": "GPT/2025/001",  // unique forever
  "name": "John Doe",
  "dob": "2010-05-15",
  "parent_phone": "08012345678",
  "results_visible": true
}














NEXT THINGS
1. Go over the workflow and clear all redundancies; keep a documentation of how things flow from collection to collection because of the future.


URGENT
2. Redesign the student profile to show the details of the student. it should include the name, class, picture (which they uploaded), number in class and studentship status (active, on probation, graduated, left, rusticated)

2. Allow the students to be able to change their passwords when they forget it.

3. For the admin enrolment page, allow the admin to be able to label some students as 'graduated' or 'left' so that any student that makes it to the enrolment page has active studentship.

4. Add breadcrumbs on every page so that people can go back to the dashboard directly

5. The broadsheet is showing zero all through, see to it. 
6. Allow  the students to be able to see their class teacher's comment too
